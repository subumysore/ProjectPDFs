{
  "name": "spike-translated-fill",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "De-risk spike for ProjectPDFs requirement #3: OCR a scanned foreign-language PDF, translate the field labels, fill in the user's language, export in original-or-chosen language. NOT production; deliberately outside the pnpm workspace.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.7.76",
    "tesseract.js": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
