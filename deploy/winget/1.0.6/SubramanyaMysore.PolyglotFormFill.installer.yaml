# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
# Installer manifest. Points winget at the signed installer on our own domain. The SHA-256 MUST match
# the exact bytes served; if the installer is ever rebuilt, regenerate this (SHA changes per build).
PackageIdentifier: SubramanyaMysore.PolyglotFormFill
PackageVersion: 1.0.6
Platform:
  - Windows.Desktop
MinimumOSVersion: 10.0.17763.0
InstallerType: nullsoft            # Tauri ships an NSIS (Nullsoft) installer
Scope: user                        # Tauri v2 NSIS default installMode is currentUser (per-user)
InstallModes:
  - interactive
  - silent
  - silentWithProgress
InstallerSwitches:
  Silent: /S                       # NSIS silent
  SilentWithProgress: /S
UpgradeBehavior: install
ReleaseDate: 2026-08-02
Installers:
  - Architecture: x64
    InstallerUrl: https://polyglotformfill.com/download/PolyglotFormFill-Setup.exe
    InstallerSha256: 82F98D8AFA4CBEABEF8C27F3C682B53309A7718ECF9D05B868065D1AC4D14D7C
ManifestType: installer
ManifestVersion: 1.6.0
