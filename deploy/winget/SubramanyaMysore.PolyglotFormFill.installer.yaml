# winget installer manifest. See SubramanyaMysore.PolyglotFormFill.yaml for submission steps.
#
# InstallerSha256 values come from docs/marketing/site/download/release-manifest.json (the same
# hashes published on the install page) — regenerate with:
#   node scripts/release-manifest.mjs generate --dir docs/marketing/site/download --version <ver>
# winget verifies the download against this hash, which is the integrity guarantee that stands in
# for a code signature while none has been purchased (ADR-0020).
#
# NOTE: the InstallerUrls must be live before submitting — winget validation downloads them.
PackageIdentifier: SubramanyaMysore.PolyglotFormFill
PackageVersion: 1.0.7
InstallerLocale: en-US
Platform:
- Windows.Desktop
MinimumOSVersion: 10.0.17763.0
Scope: user
InstallModes:
- interactive
- silent
UpgradeBehavior: install
ReleaseDate: 2026-07-23
# Only the NSIS installer is published. The MSI is still built by `tauri build`, but it is no
# longer hosted (it duplicated the .exe for ~32 MB), and winget validation downloads InstallerUrl —
# so an unhosted MSI cannot be offered here.
Installers:
- Architecture: x64
  InstallerType: nullsoft
  InstallerUrl: https://polyglotformfill.com/download/PolyglotFormFill-Setup.exe
  InstallerSha256: 47953F25A450A9A88AA11FB151D77EECDBC089BC0FB9F8F310CABFB7F9BDDBE7
ManifestType: installer
ManifestVersion: 1.6.0
