<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the macOS build (required for notarization).
  Kept MINIMAL on purpose — this is a privacy-first, on-device app: no network-server,
  no camera/mic/location entitlements are granted here (add them only if a feature needs one).
  The JIT entitlements are what the WebView's JavaScript engine needs under hardened runtime.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
