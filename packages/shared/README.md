# packages/shared — shared TS types & schemas

Types/schemas shared across the UI (`apps/app/src`), workers, and the Node services — and mirrored
from the Rust core's public interfaces where they cross the bridge. Node-buildable; can start now
alongside `apps/app/src`. Keep a single source of truth per concept (governance §1).
