# Requirements Traceability Matrix

_The SINGLE source of truth for requirement status. Every `REQ-NN.M` in the BRD appears here exactly
once. `node scripts/check-traceability.mjs` reconciles this against the BRD._

## Legend
- **Status:** ☐ Not started · 🟡 In progress · ✅ Completed
- **Prod-Ready:** ✅ yes (tests + coverage meet bar) · ⚠️ coded, manual-verified, no full automated test ·
  🟠 tested with gaps · ❌ no
- **Coverage %** from the coverage report.

## Executive rollup
| Total | ✅ | 🟡 | ☐ | Prod-Ready ✅ | ⚠️ | 🟠 | ❌ |
|---|---|---|---|---|---|---|---|
| 18 | 0 | 18 | 0 | 0 | 16 | 0 | 2 |

## Matrix
| REQ | Title | Status | Implementation | Tests | Cov % | Prod-Ready |
|---|---|---|---|---|---|---|
| REQ-01.1 | On-device vault + catalog-first autofill | 🟡 | `core-store` (values AES-GCM sealed), `core-crypto`, `core-catalog`. **2026-07-22:** extension⇄desktop **automatic single vault** — desktop owns `vault.db`, `native-host` bridges it, extension `resolveVaultMode` auto-detects (no toggle) + order-independent safe-union migration (`companion.js`). **2026-07-23 (later):** deleting a profile now REQUIRES the vault passphrase — `delete_profile` verifies it server-side (shared `verify_passphrase`, also used by `unlock`) behind the two-step confirm; wrong/empty passphrase deletes nothing and shows an inline localized error (`profile.removePassWrong`, all 26 langs). Vault rows zebra-striped + tightened for readability. | `core-store` (incl. encrypted-at-rest), `core-catalog`, `core-crypto` unit tests; **`companion.test.mjs` (+5: seeding/no-clobber/deferral)**; acceptance `features/single-shared-vault.feature`. **Native-messaging round-trip = @live gate** | — | ⚠️ |
| REQ-02.1 | Make fillable: CREATE fields on flat PDFs + fill/export | 🟡 | `core-catalog` field-maps WITH coordinates; app creates AcroForm widgets on FLAT PDFs + fills + exports (pdf-lib), fills existing fields + pdf.js render. **Extension/desktop intelligent fill (2026-07-20):** layered — AcroForm field-NAME templates (W-4/W-9), semantic resolver by field label (tooltip→name; compose name, middle initial, **age from DOB**, split SSN, combine/split address), OCR-draw for XFA/scanned (W-2), form recognition (`pdfforms.js`); **image-valued fields** (photo/signature drawn into PDF boxes); **interactive filled-PDF viewer** (`<iframe>`, unfilled fields stay editable). **Desktop 2026-07-22:** **review & edit** step after fill (`listReviewFields`/`applyReviewEdits` — text/radio/checkbox/dropdown, correct-before-finalize, no silent commit), matching the extension's editable viewer. **2026-07-23 (fill-safety pass):** non-Latin values now FILL — per-field appearance generation with an embedded script font (`appearances()` in `pdffill.js`, desktop twin `apps/app/src/fill/appearances.ts`), so a CJK value no longer aborts the whole export; derived/office-use boxes (check digit, official-use) blocked by caption OR field name; a qualified address (correspondence/mailing/office) never mirrors the residential one; the extension gained the desktop's ask-before-saving capture (`pagecapture.js`). **2026-07-23 (later):** `makeFillableAndFill` (flat→fillable) composed no name — it filled by exact `vault[ontology_key]`, bypassing the resolver, so a `full_name` field with only `first_name`/`last_name` in the vault stayed blank; now falls back to the semantic resolver. Resolver also falls back to a field's programmatic NAME when the label is unhelpful (guarded: a confident label match, e.g. `Company name`, is never overridden). Detect-fields now opens the review editor so a value the vault lacked (Nationality) can be typed and auto-registers. See ADR-0016 | catalog tests; `notmine.test.mjs` (10 — office-use/check-digit/qualified-address, both engines), `appearances.test.mjs` (4, desktop, against the real shipped Noto fonts), `pagecapture.test.mjs` (6), engine-parity safety-rule guards; W-4/W-9 render-verified (5/5, 6/6); W-2 OCR + image-field render-verified; resolver/age unit-tested; acceptance `features/review-before-finalize.feature` | — | ⚠️ |
| REQ-03.1 | Language-aware filling (native lang; view + write-back) | 🟡 | on-device NMT (transformers.js/ONNX, self-hosted, zero egress). **2026-07-20:** spec ETCHED (`docs/specs/language-aware-filling.md`, ADR-0016) — **native_language is a VAULT PROFILE field**; form-language AUTO-DETECT (`lang.js`, 8 langs); any-to-any via **English pivot**; **bilingual side panel**; output ALWAYS in the form's original language; **Devanagari/CJK output fonts** (fontkit + hosted Noto). Desktop `translate.ts` + `App.tsx` brought to parity (8 langs, pivot, persisted native_language). | `lang.js` detect unit-verified (8/8); Devanagari font render-verified; desktop tsc+build green. **Translate RUNTIME (model load) unvalidated in a real browser; non-en/hi desktop models need provisioning into public/models** | — | ❌ |
| REQ-04.1 | Auto-tags + on-device index/search | 🟡 | `core-catalog` search + tags; `services/catalog`; app `catalog_search` + UI | `core-catalog` search tests | — | ⚠️ |
| REQ-05.1 | Native cross-platform app, data on-device | 🟡 | `apps/app` (Tauri v2 + Rust core). **2026-07-22:** real **release build** produces installers (`PolyglotFormFill_0.1.0` MSI 31 MB + NSIS 29 MB) after fixing `E0786` (embedded 1.3 GB models → rlib bloat; `lto=false`). Models now runtime-provisioned (not bundled) | frontend build + `cargo build` + **`cargo tauri build` → MSI/NSIS bundles**. Windows only so far; macOS/Linux bundles pending | — | ⚠️ |
| REQ-05.2 | Distribution trust without a paid code-signing certificate | 🟡 | **2026-07-23 (ADR-0020, `docs/specs/release-integrity.md`):** GA no longer blocked on buying a certificate. Public releases ship **deliberately unsigned** — self-signing is dev-only (invalid chain ⇒ no SmartScreen benefit, and worse under some AV/enterprise policy). Trust carried instead by (a) **extension-first** channel ordering, (b) **winget** (accepts unsigned `.exe`/`.msi`; MSIX deliberately avoided as it *would* require a paid cert), (c) **published SHA-256 per artifact** via `scripts/release-manifest.mjs` (generate/verify, deterministic, fails loudly on an empty dir), (d) install page **predicts the SmartScreen warning before download** with the exact click path + hash-verification command, reading `release-manifest.json` at runtime so copy cannot drift. Upgrade = set `WINDOWS_CERT_THUMBPRINT` (one line). **Artifacts rebuilt at 1.0.0** (setup .exe 29.9 MB + MSI 31.9 MB + ext zip 9.0 MB); versions read back from the artifacts themselves (PE VersionInfo; MSI Property table) and both confirmed `NotSigned`. **winget manifests written** (`deploy/winget/`, 3 files, `winget validate` passes). **2026-07-25 (ADR-0023 + ADR-0024):** strategy consolidated — ADR-0023 records the ordered Windows trust plan + honest macOS feasibility (deferred, needs a build Mac + Apple Developer Program US$99/yr + notarization) + paid-signing order (Azure Trusted Signing → OV → EV); ADR-0024 (roadmap, extends 0023) defers paid platform work and SEQUENCES it around registering a business entity (EV cert → Azure org re-check → macOS → Microsoft Store/MSIX), presents desktop + extension EQUALLY, and prioritises language packs by global speaker volume (QA/pre-staging only — NLLB is on-device any-to-any per ADR-0018, never a per-pair gate). Triggers = revenue + entity registered; no dates | `scripts/release-manifest.test.mjs` **12 tests** + `scripts/winget-manifest.test.mjs` **5 tests** (hash-drift guard, proven to fail on a corrupted hash) (known-vector hash, sort, determinism, empty-dir throw, OK/MISMATCH/MISSING, all-problems-in-one-pass, `signed:false`) wired into `scripts/test-all.mjs`; CLI exercised end-to-end (exit 0 clean / exit 1 tampered); acceptance `features/release-integrity.feature`. **Rendered install page + a real first-run install = @live gate** | — | ⚠️ |
| REQ-06.1 | AI search bar | 🟡 | app "Find a form" search UI + `catalog_search` | core-catalog search tests | — | ⚠️ |
| REQ-07.1 | Save (versioned) & submit | 🟡 | `save_filled_form` (versions) + PDF export (pdf-lib) + `open_submit_url` (opens vendor page, HTTP warning, never proxied). **2026-07-22:** brought forms (device/URL/search) auto-saved to Past forms — `save_brought_form`/`list_saved_forms`/`saved_form_pdf` + `form_blobs` (sealed filled-PDF bytes) | core-store versioning tests; **`brought_form_blobs_and_instance_listing` (blob round-trip + sealed-at-rest + instance listing)**; acceptance `features/past-forms-history.feature`; builds. Fill→save verified LIVE (Japan visa, 12/60→v1). **Full click-through = @live gate** | — | ⚠️ |
| REQ-08.1 | History, versioning, annotation layers | 🟡 | `core-store` FormInstance + immutable encrypted versions + history counters + `form_blobs`. **2026-07-22:** desktop **Past forms** UI (re-download + sign) and **Sign / annotate** tool (`SignPad.tsx`: pen colour/size, text, movable+resizable image stamps, undo, multi-page → shared `flattenOverlays`) | `core-store` versioning/history/blob tests; shared `signflatten` engine; acceptance `features/sign-annotate-desktop.feature` + `features/past-forms-history.feature`. **Interactive canvas = @live gate** | — | ⚠️ |
| REQ-09.1 | Non-delegable signing (Tier 1) | 🟡 | app `sign_form` + **`sign_saved_form` (2026-07-22)**: device Ed25519 key (OS keystore) signs the version hash + provenance -> signatures table + UI (Past forms "Sign"). OIDC broker for federated identity (reused). Tier-2 biometric + live OIDC pending | core-store signatures + core-crypto + account OIDC tests; sign verified LIVE (Past forms, doc-hash shown); acceptance `features/past-forms-history.feature` | — | ⚠️ |
| REQ-10.1 | Data-source extraction + profiles | 🟡 | app on-device OCR (tesseract.js) + heuristic extractor -> vault; profiles/subscriptions modeled. **Extension (2026-07-20):** camera/image capture → OCR (`tess.js`/`parse.js`) with glossy-ID preprocessing + driver's-licence heuristics (unlabelled + AAMVA field numbers); **PDF417 back-of-licence barcode → exact AAMVA fields** (`@zxing` vendored + `parseAamva`), LIVE continuous scan + continuous-autofocus (blur-tolerant); review-and-save UI. **2026-07-23 (later):** desktop `ocr.ts` parser fixed — ID cards print `Label value` with a single space and no colon, so only ONE field extracted and the licence number was mislabelled as a phone; now single-space matching (longest label wins) + `license_no`/`id_no`/`expiry_date` labels + phone fallback suppressed when an ID/licence/passport number is present. A licence yields name/DOB/licence no/address/city/expiry in one import (verified live). See ADR-0016. **2026-07-25 (extension parity + SSOT):** the desktop "Import a data source" path now RETAINS THE WHOLE DOCUMENT IMAGE too (not only text) — the extension already did this per `docs/specs/document-image-fields.md`; the desktop lagged. One pure classifier `documentImageKey(fields,isBarcodeBack)` (`apps/app/src/ocr.ts`) keys the image under the SAME shared-vault ontology (`driver_license_front`/`driver_license_back`/`passport_image`/`document_image`), rendered as a thumbnail + default-on checkbox before "Save N to vault"; corrected a brief `*_scan` key divergence that would have broken the shared vault | ext: barcode+OCR validated end-to-end in a loaded extension (11 exact fields); `parseAamva`/`parseFields` unit-tested; **`apps/app/src/ocr.test.mjs` (5: ID card, longest-label, colon-form + real phone, header-only, `documentImageKey` shared-ontology)**; live-camera wiring needs on-device test | — | ⚠️ |
| REQ-11.1 | Web-hosted download + web-form autofill | 🟡 | `core-fetch` URL validation (SSRF guard) + `core-webform` fill-plan; webview DOM injection pending. **2026-07-25 — marketing site fully localized (all 26 UI langs):** `docs/marketing/build-site.mjs` rewritten to render the COMPLETE landing + privacy + install pages per language from one template set + a per-language catalogue (`docs/marketing/i18n/<lang>.json`, 188 keys; SSOT for site copy, shared `app.*`/`privacy.*`/`site.*` still from `apps/extension/src/i18n.js`), replacing the old ~3 KB stubs. Language switcher on every page + language-preserving nav/footer/switcher links; RTL for ar/he/fa/ur; Hello bg + PPP + install version injection + SEO meta preserved. Built LOCAL only — not published (owner reviews) | core-fetch + core-webform tests; **`docs/marketing/check-i18n.mjs`** (key-set/type/array-length/`{each}` parity, 25/25 pass) | — | ⚠️ |
| REQ-12.1 | Multi-party documents | 🟡 | `core-txn` state machine (roles, consent, sign, re-sign-on-edit) | `core-txn` workflow tests | — | ⚠️ |
| REQ-13.1 | Verifiable provenance (authority-scoped) | 🟡 | `core-crypto` ProvenanceManifest (SHA-256 + Ed25519 sign/verify); authority-encrypted block pending | provenance sign/tamper tests | — | ⚠️ |
| REQ-14.1 | Registered roles & verifiable workflows | 🟡 | `core-identity` Registry + Role capabilities (Sign non-delegable); workflow binding pending | `core-identity` role/capability tests | — | ⚠️ |
| REQ-17.1 | Offline monetization (licensing) | 🟡 | `core-license`: Ed25519-signed license tokens (tier/features/expiry) verified on-device against the embedded vendor public key; no activation server, nothing sent. Verify command + import UI pending. See RFC-0005 / ADR-0015. **2026-07-28 (payments LIVE on Stripe — ADR-0025):** Lemon Squeezy stalled in pending-approval, so we shipped **Stripe** (owner=merchant of record, Stripe Tax active). Live account `acct_1KoDEkFWVcXPDpjx`: Pro $19 / Duo $29 one-time, Business $29/seat/yr (adjustable 1–19); PPP coupons + `PPP{band}` promo codes (shown==charged); hosted Payment Links with a `device_id` field + auto-tax + redirect to the claim page. **Zero-storage issuer** re-mints the token from the paid Stripe Checkout Session (`issuer-server.mjs`) with `Stripe-Signature` webhook verify (`stripe-webhook.mjs`) — SAME production signing key, so every shipped app/extension keeps verifying. Deployed to OKE (code-rev 2); live endpoints smoke-tested. Site rebuilt 26 langs, `stripe-config.json` live:true. **2026-07-28 (in-app Buy):** desktop License panel + extension popup now show **Buy Pro/Duo/Business** buttons that open Stripe Checkout with this install's device id as `client_reference_id` → the licence auto-binds to THIS device (buyer types nothing); issuer prefers `client_reference_id` over the typed field. Desktop **1.0.2** (installer re-published w/ fresh SHA-256 manifest); extension carries the popup change but manifest stays **1.0.3** until the in-review build clears (then republish as 1.0.4). **2026-07-28 (Windows signing LIVE — ADR-0026):** installer Authenticode-signed via **Azure Trusted Signing** (publisher `Subramanya Mysore` → Microsoft Identity Verification Root CA 2020, timestamped); `sign-windows.ps1` Trusted-Signing mode; `release-manifest.mjs --signed` flips the install page to "Digitally signed". Client secret renews ~2027-01-24. **2026-07-28 (7-DAY TRIAL ENFORCED — ADR-0027):** no free-forever tier — `GET /issuer/trial?device=` mints a device-bound 7-day full-features token; desktop `ensure_trial`+`requireEntitlement()` gate the 5 fill/export handlers (**1.0.3 signed**), extension `ensureTrial()`/`isActive()` gate popup-Fill + auto-fill (ships store **1.0.4**). Privacy intact (only random device id leaves; issuer stateless). **Open:** owner end-to-end purchase test; extension store republish (1.0.3 in review); translate `license.buy*`. | `core-license` tests (5: issue↔verify+gating, wrong-key, tamper, expiry/perpetual, malformed); **`scripts/license/issuer-server.test.mjs` (4: Stripe re-mint/email-mismatch/unpaid→404, empty→form, **trial mint device-bound 7-day**)**; **`scripts/license/stripe-webhook.test.mjs` (3)** — license suite; **`cargo check` + `tsc` clean** | `core-license` tests (5: issue↔verify+gating, wrong-key, tamper, expiry/perpetual, malformed); **`scripts/license/issuer-server.test.mjs` (3: re-mint device-bound from paid session, email-mismatch/unpaid/unknown→404, empty→form)**; **`scripts/license/stripe-webhook.test.mjs` (3: valid sig in window, tamper/wrong-secret/stale→reject, issue/revoke/ignore routing)** — license suite 12/12 | — | ⚠️ |
| REQ-16.1 | Browser-extension client (secure) | 🟡 | `apps/extension` MV3: `vault.js` (AES-256-GCM; key derived on unlock via PBKDF2 passphrase or WebAuthn-PRF, never stored, non-extractable, memory-only) + background SW + popup (passphrase/passkey unlock, fill/lock) + page autofill + **passkey enrolment** (options). **Companion bridge:** `crates/native-host` (`projectpdfs-host`) native-messaging binary reads the app's on-device vault (same keystore key/DB); extension pulls the vault over the bridge so keys/vault stay in the native trust anchor; app **auto-registers** the companion (`register_companion` command + winreg; host bundled via `beforeBundleCommand` + `resources`); **passphrase→passkey migration** (re-seal under WebAuthn key). Engine reuse + reproducible builds pending. See RFC-0004 / ADR-0014 | `vault.test.mjs` (6, incl. migration) + `native-host` framing (2) + app compiles + JS syntax-checked | — | ⚠️ |
| REQ-15.1 | Multi-format forms (Word/Excel) | 🟡 | app `office.ts`: on-device fill of Word content controls + Excel named ranges (Phase A) AND flat docs — table label→next cell, "Label:" paragraphs, Excel label→neighbour cell (Phase B); fflate + fast-xml-parser round-trip; `.docx/.xlsx` routed in "Fill a Form". Office→PDF content export on-device (Phase C Tier-1, pdf-lib) via "Export as PDF". Tier-2 (LibreOffice)/vector print pending. See RFC-0002/0003, ADR-0011/0012 | `office.test.mjs` (7 tests incl. officeToPdf) + acceptance `features/office-form-fill.feature` (8 scenarios) + frontend build + headless UI run (fill + Export-as-PDF) | — | ⚠️ |
